name: Auto deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    uses: moogur/all-workflows/.github/workflows/auto_deploy_for_docker_container.yml@master
    secrets: inherit
    environment: ${{ vars.DEPLOY }}
    with:
      repository_url: "https://github.com/yumata/lampa.git"
      repository_branch: "main"
      last_update_commit_time: ${{ LAST_UPDATE_COMMIT_TIME }}
